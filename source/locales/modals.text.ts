export default class MODALES_LOCALE {
    static gtToggle1(): { name: string, desc: string } {
        return {
            name: "Include in extensions registry:",
            desc: "If enabled, generated file's extension would be inserted in extensions array.",
        };
    }
}
